const Photo = () => {
    return (
        <div>
            <h2>
                URL route with underscore (_) specify the folder name with (%5F)
            </h2>
        </div>
    )
}

export default Photo;